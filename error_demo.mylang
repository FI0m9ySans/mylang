# 错误处理演示

打印("=== 错误处理演示 ===");

# 1. 未定义变量错误演示
打印("1. 未定义变量错误:");
尝试 {
  打印(未定义变量);
} 捕获 {
  打印("捕获到错误: " + 错误信息);
}

# 2. 类型错误演示
打印("2. 类型错误演示:");
尝试 {
  结果 = 10 - "字符串";
  打印(结果);
} 捕获 {
  打印("捕获到类型错误: " + 错误信息);
}

# 3. 除以零错误演示
打印("3. 除以零错误演示:");
尝试 {
  结果 = 10 / 0;
  打印(结果);
} 捕获 {
  打印("捕获到算术错误: " + 错误信息);
}

# 4. 语法错误演示（这个会在解析时就报错）
打印("4. 语法错误演示:");
尝试 {
  # 这个语句缺少分号，会在解析时报错
  打印("缺少分号")
} 捕获 {
  打印("捕获到语法错误: " + 错误信息);
}

打印("=== 演示结束 ===");